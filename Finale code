1. Data exploration

```{r setup, include=FALSE}
#install.packages("tidyverse")
library(tidyverse)
```
```{r}
getwd()
genera_counts <- read_tsv("~/BIOMED/MJ1/Large scale analysis/Data project/genera.counts.tsv")
metadata <- read_tsv("~/BIOMED/MJ1/Large scale analysis/Data project/metadata.tsv")
```

```{r}
head(genera_counts) #eerste paar rijen bekijken
head(metadata) #eerste paar rijen bekijken
dim(genera_counts) #dimensies bekijken (aantal rijen en kolommen)
dim(metadata)
colnames(genera_counts) #kolomnamen bekijken
colnames(metadata)
```
